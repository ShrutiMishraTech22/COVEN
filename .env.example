# Copy this file to .env and fill in real values before running with Postgres.
# For local dev without Postgres, leave DATABASE_URL unset — the app
# defaults to a local SQLite file (backend/trustgraph.db).

DATABASE_URL=postgresql://trustgraph:trustgraph@localhost:5432/trustgraph
